<%= form_with(model: recipe) do |form| %>
  <% if recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recipe.errors.count, "error") %> prohibited this recipe from being saved:</h2>

      <ul>
        <% recipe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, id: :recipe_title, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :description %>
    <%= form.text_area :description, id: :recipe_title, class: "form-control" %>
  </div>

  <h3> Add Ingredients </h3>
  <%= form.fields_for :ingredients do |ingredient| %>
      <% render 'ingredient_fields', form: ingredient %>
    <% end %>
    <div class="links">
    <%= link_to_add_association "Add Ingredient", form, :ingredients  %>
    </div>


  <h3> Add Steps </h3>
  <div class="form-group">
    <%= form.fields_for :steps do |step| %>
      <%= ingredient.text_field :direction %>
      <%= ingredient.remove_nested_fields_link %> 

    <% end %>
      <%= form.add_nested_fields_link :steps %>
  </div> 


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
